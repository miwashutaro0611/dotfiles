# Brewfile — install with `brew bundle --file=Brewfile`
#
# Use `brew bundle check --file=Brewfile` to see what's missing.
# Comment out lines for apps you don't need on a given machine.

# ─── CLI tools ────────────────────────────────────────────
brew "bat"             # cat with syntax highlighting
brew "eza"             # modern ls replacement
brew "gh"              # GitHub CLI
brew "git"
brew "lazygit"         # terminal UI for git
brew "mas"             # Mac App Store CLI
brew "mise"            # polyglot runtime version manager
brew "peco"            # interactive filtering (used in zsh history search)
brew "starship"        # cross-shell prompt
brew "zsh-completions"

# ─── Package managers / runtimes ──────────────────────────
brew "ni"              # universal npm/yarn/pnpm wrapper
brew "rbenv"           # Ruby version manager
brew "uv"              # fast Python package manager

# ─── Mobile / native dev ──────────────────────────────────
brew "cocoapods"

# ─── Media ────────────────────────────────────────────────
brew "ffmpeg"

# ============================================================
# GUI Applications (Casks)
# ============================================================

# ─── Browsers ─────────────────────────────────────────────
cask "arc"
cask "google-chrome"

# ─── Terminals ────────────────────────────────────────────
cask "ghostty"
cask "iterm2"
cask "warp"

# ─── Editors / IDEs ───────────────────────────────────────
cask "cursor"
cask "kiro"
cask "rustrover"
cask "webstorm"
# cask "visual-studio-code"

# ─── AI tools ─────────────────────────────────────────────
cask "chatgpt"
cask "claude"          # Claude desktop app
cask "claude-code"
cask "codex"

# ─── Communication ────────────────────────────────────────
cask "discord"
cask "slack"
cask "zoom"

# ─── Productivity ─────────────────────────────────────────
cask "clipy"           # clipboard manager
cask "jordanbaird-ice" # menu bar manager
cask "notion"
cask "raycast"

# ─── Design ───────────────────────────────────────────────
cask "figma"
cask "framer"
cask "pencil"

# ─── Utilities ────────────────────────────────────────────
cask "keka"            # archive utility

# ─── Mobile / native dev ──────────────────────────────────
cask "android-studio"
cask "flutter"
cask "unity-hub"

# ============================================================
# Mac App Store apps (via mas — requires Apple ID sign-in)
# ============================================================
# Run `mas signin <apple-id>` once before `brew bundle`.
# IDs were taken from `mas list` on a machine that already has them installed.

# mas "LINE",            id: 539883307
# mas "Magnet",          id: 441258766
# mas "Amazon Kindle",   id: 302584613
# mas "Display Menu",    id: 549083868
# mas "⌘英かな",          id: 925919730

# ============================================================
# Apps NOT available via brew / mas (manual install required)
# ============================================================
# - Adobe Creative Cloud / Lightroom CC  → Adobe website
# - Affinity Designer / Photo            → Mac App Store (paid)
# - Final Cut Pro / Motion               → Mac App Store (paid)
# - Xcode                                → Mac App Store (or developer.apple.com)
# - Dia browser                          → https://www.diabrowser.com/
# - Imaging Edge (Sony)                  → Sony website
# - Elgato Game Capture HD               → Elgato website
